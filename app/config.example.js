// Copy this file to `config.js` and fill in your own Supabase project values.
// Find them in Supabase → Project Settings → API.
// The anon key is safe to expose in a browser (Row Level Security protects your data) —
// safe to commit this file to git, unlike a real secret key.
window.APP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
